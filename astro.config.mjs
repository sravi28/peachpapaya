// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://peachpapaya.vercel.app", // Update this with your actual Vercel URL
  outDir: "./dist",
});
